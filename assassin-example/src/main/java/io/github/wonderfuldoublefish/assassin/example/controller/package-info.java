/**
 * assassin-example 控制器包。
 *
 * <p>提供用于演示 assassin-web 自动装配效果的 REST 接口。</p>
 *
 * <p>本包启用 JSpecify 空安全标记：默认按非空对待，允许为空的元素一律显式标注 {@code @Nullable}。</p>
 */
@NullMarked
package io.github.wonderfuldoublefish.assassin.example.controller;

import org.jspecify.annotations.NullMarked;
