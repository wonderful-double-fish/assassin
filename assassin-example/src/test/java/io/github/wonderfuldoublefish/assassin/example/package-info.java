/**
 * assassin-example 测试包。
 *
 * <p>存放端到端冒烟测试，验证 assassin-web starter 在真实应用中的自动装配效果。</p>
 *
 * <p>本包启用 JSpecify 空安全标记：默认按非空对待，允许为空的元素一律显式标注 {@code @Nullable}。</p>
 */
@NullMarked
package io.github.wonderfuldoublefish.assassin.example;

import org.jspecify.annotations.NullMarked;
